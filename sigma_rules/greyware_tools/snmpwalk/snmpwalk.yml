title: Simple keyword detection rule for snmpwalk
id: afeed36b-305f-4b42-a6ce-d6b3ba534b31
status: experimental
description:
- '''allows you to enumerate the SNMP devices and places the output in a very human
  readable friendly format'''
references:
- https://wiki.debian.org/SNMP
author: '@mthcht'
date: 2023/07/30
modified: 2023/08/28
tags:
- attack.T1018
- attack.T1046
- attack.TA0005
- attack.TA0007
logsource:
  category:
  - endpoint
falsepositives:
- Unknown
level:
- medium
detection:
  selection:
    ParentCommandLine:
    - '*snmpwalk  -v1 -cpublic *'
    CommandLine:
    - '*snmpwalk  -v1 -cpublic *'
    Details:
    - '*snmpwalk  -v1 -cpublic *'
    registry.value:
    - '*snmpwalk  -v1 -cpublic *'
    process.args:
    - '*snmpwalk  -v1 -cpublic *'
    process.command_line:
    - '*snmpwalk  -v1 -cpublic *'
    process.env_vars:
    - '*snmpwalk  -v1 -cpublic *'
    process.io.text:
    - '*snmpwalk  -v1 -cpublic *'
    process.title:
    - '*snmpwalk  -v1 -cpublic *'
    pe.company:
    - '*snmpwalk  -v1 -cpublic *'
    pe.description:
    - '*snmpwalk  -v1 -cpublic *'
    pe.product:
    - '*snmpwalk  -v1 -cpublic *'
    os.full:
    - '*snmpwalk  -v1 -cpublic *'
    host.hostname:
    - '*snmpwalk  -v1 -cpublic *'
    event.original:
    - '*snmpwalk  -v1 -cpublic *'
    email.subject:
    - '*snmpwalk  -v1 -cpublic *'
    device.model.name:
    - '*snmpwalk  -v1 -cpublic *'
    container.image.name:
    - '*snmpwalk  -v1 -cpublic *'
    container.name:
    - '*snmpwalk  -v1 -cpublic *'
  condition: selection
fields:
- ParentCommandLine
- CommandLine
- Details
- registry.value
- process.args
- process.command_line
- process.env_vars
- process.io.text
- process.title
- pe.company
- pe.description
- pe.product
- os.full
- host.hostname
- event.original
- email.subject
- device.model.name
- container.image.name
- container.name
